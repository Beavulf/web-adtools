# ⚡ Быстрый старт - Деплой на Windows сервер

## Самый простой способ (IIS)

### 1. Подготовка

```bash
# Создайте файл .env.production
VITE_APP_URI_GRAPHQL=http://your-server-ip:4000/graphql

# Соберите проект
npm run build:prod
```

### 2. Установка IIS

1. Откройте **Панель управления** → **Программы** → **Включение компонентов Windows**
2. Включите **Службы IIS (Internet Information Services)**
3. Скачайте и установите [IIS URL Rewrite Module](https://www.iis.net/downloads/microsoft/url-rewrite)

### 3. Размещение файлов

1. Скопируйте **все файлы** из папки `dist/` в папку `C:\inetpub\wwwroot\web-adtools\`
2. Убедитесь, что файл `web.config` также скопирован

### 4. Создание сайта в IIS

1. Откройте **Диспетчер IIS**
2. Правой кнопкой на **Sites** → **Add Website**
   - Site name: `web-adtools`
   - Physical path: `C:\inetpub\wwwroot\web-adtools`
   - Port: `80` (или другой)

### 5. Готово!

Откройте в браузере: `http://your-server-ip:80`

---

## Автоматический деплой

Используйте PowerShell скрипт:

```powershell
.\deploy.ps1
```

Скрипт автоматически:
- ✅ Проверит .env.production
- ✅ Соберет проект
- ✅ Скопирует файлы на сервер

**Настройка пути:**
```powershell
.\deploy.ps1 -DeployPath "C:\inetpub\wwwroot\web-adtools"
```

---

## Что делать если что-то не работает?

📖 Полная инструкция со всеми вариантами и решением проблем: **[DEPLOYMENT.md](./DEPLOYMENT.md)**

