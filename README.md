# WEB AD Tools

Полноценное веб-приложение для управления пользователями Active Directory: поиск сотрудников, автоматическая блокировка и разблокировка учетных записей при уходе в отпуск, стажировку, декрет, командировку и т.д. Включает отслеживание процессов и ведение статистики.

## 🚀 Технологический стек

### Frontend
- **React 19** — библиотека для создания пользовательских интерфейсов
- **Vite** — сборщик и dev-сервер
- **Apollo Client** — клиент для работы с GraphQL
- **Ant Design** — UI-библиотека компонентов
- **styled-components** — CSS-in-JS решение
- **React Router DOM** — маршрутизация
- **dayjs** — работа с датами

### Backend (требуется отдельный сервер)
- **NestJS** — Node.js фреймворк
- **GraphQL** — API для запросов данных
- **Prisma** — ORM для работы с базой данных
- **PostgreSQL** — реляционная база данных
- **JWT** — токены для авторизации
- **LDAP** — интеграция с Active Directory

## ✨ Основной функционал

- 🔐 Авторизация через LDAP с использованием учетных записей Windows
- 👤 Поиск пользователей в Active Directory
- 🔒 Автоматическая блокировка/разблокировка учетных записей
- 📅 Планирование блокировок (отпуска, декрет, командировки, стажировки)
- 📊 Ведение статистики выполненных и неудачных операций
- 🕐 Отслеживание расписания автоматических задач
- ⚙️ Настройка сервисов и параметров системы
- 📝 История изменений по каждому пользователю

## 📋 Требования

- **Node.js** версии 18 или выше
- **npm** или **yarn** для управления зависимостями
- Доступ к серверу бэкенда (NestJS с GraphQL)
- Доступ к Active Directory (LDAP) через бэкенд

## 🔧 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <your-repository-url>
cd web-adtools
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
VITE_APP_URI_GRAPHQL=http://localhost:4000/graphql
```

**Важно:** Замените URL на адрес вашего GraphQL сервера.

### 4. Запуск dev-сервера

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173` (или другому порту, который укажет Vite).

### 5. Сборка для production

```bash
npm run build
```

Готовая сборка будет находиться в папке `dist/`.

Для предпросмотра production-сборки:

```bash
npm run preview
```

## 📁 Структура проекта

```
web-adtools/
├── src/
│   ├── assets/              # Статические ресурсы (изображения, иконки)
│   ├── context/             # React контексты (AuthContext, MessageContext)
│   ├── hooks/               # Кастомные хуки
│   │   ├── api/            # Хуки для работы с API (useLdap, useRecall, useSchedule)
│   │   └── user/           # Хуки для работы с пользователями
│   ├── pages/               # Страницы приложения
│   │   ├── login/          # Страница авторизации
│   │   └── home/           # Главная страница с основным функционалом
│   │       └── components/ # Компоненты страницы Home
│   │           ├── layout/          # Компоненты макета (Header, Sider, Footer)
│   │           ├── schedule/        # Компоненты для работы с расписанием
│   │           ├── schedule-statistics/  # Статистика расписания
│   │           ├── service-settings/     # Настройки сервисов
│   │           └── user/            # Компоненты для работы с пользователями
│   ├── query/               # GraphQL запросы и мутации
│   ├── services/            # Сервисы (Apollo Client, и др.)
│   ├── utils/               # Вспомогательные функции
│   ├── App.jsx              # Главный компонент приложения
│   └── main.jsx             # Точка входа в приложение
├── public/                  # Публичные файлы
├── package.json
├── vite.config.js          # Конфигурация Vite
└── README.md
```

## 🛠 Команды разработки

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev-сервера с hot-reload |
| `npm run build` | Сборка приложения для production |
| `npm run preview` | Предпросмотр production-сборки |
| `npm run lint` | Проверка кода с помощью ESLint |

## 🔐 Авторизация

Приложение использует комбинированную авторизацию:

1. **LDAP** — проверка учетных данных в Active Directory (выполняется на бэкенде)
2. **JWT** — токен для последующих запросов (хранится в `localStorage`)

### Как это работает:

1. Пользователь вводит логин и пароль от своей учетной записи Windows
2. Бэкенд проверяет учетные данные через LDAP
3. При успешной проверке сервер возвращает JWT-токен
4. Токен сохраняется в `localStorage` и добавляется в заголовок всех GraphQL запросов
5. При истечении токена пользователь автоматически перенаправляется на страницу входа

**Важно:** Не авторизованные пользователи не могут получить доступ к основным страницам приложения.

## 🎨 Стилизация

- **Ant Design** используется как основа UI-компонентов
- Постепенный переход с CSS-файлов на **styled-components**
- Все компоненты адаптивны и поддерживают различные размеры экранов
- Локализация: русский язык (Ant Design ruRU)

## 📝 Разработка

### Стиль кода

- Используется чистый JavaScript (без TypeScript)
- Все компоненты функциональные (с хуками)
- Именование: `camelCase` для переменных, `PascalCase` для компонентов
- Все бизнес-операции вынесены в хуки (`hooks/`) или сервисы (`services/`)
- Все запросы к серверу выполняются через Apollo Client

### Структура компонентов

- Максимальная вложенность JSX: 3 уровня
- Разделение крупных компонентов на мелкие логические блоки
- Все важные блоки кода содержат комментарии

### GraphQL

Все запросы к серверу выполняются через GraphQL с использованием Apollo Client. Запросы и мутации вынесены в отдельные файлы в папке `query/`.

Пример структуры:

```javascript
// query/RecallQuery.js
export const GET_RECALLS = gql`
  query GetRecalls {
    getRecalls {
      id
      username
      # ... другие поля
    }
  }
`;
```

## 🔗 Связь с бэкендом

Клиентское приложение требует запущенный бэкенд-сервер (NestJS) с GraphQL endpoint. 

Все запросы отправляются на адрес, указанный в переменной окружения `VITE_APP_URI_GRAPHQL`.

Токен авторизации автоматически добавляется в заголовок каждого запроса:

```
Authorization: Bearer <your-jwt-token>
```

## 📦 Деплой на локальный Windows сервер

Подробная инструкция по развертыванию приложения на локальном Windows сервере находится в файле **[DEPLOYMENT.md](./DEPLOYMENT.md)**.

### Краткая инструкция:

1. **Сборка проекта:**
   ```bash
   npm run build:prod
   ```

2. **Настройка переменных окружения:**
   - Создайте файл `.env.production` с переменной `VITE_APP_URI_GRAPHQL`

3. **Выбор веб-сервера:**
   - **IIS** (рекомендуется для Windows) - смотрите [DEPLOYMENT.md](./DEPLOYMENT.md#вариант-1-iis-internet-information-services)
   - **Nginx** - смотрите [DEPLOYMENT.md](./DEPLOYMENT.md#вариант-2-nginx-for-windows)
   - **Node.js serve** - смотрите [DEPLOYMENT.md](./DEPLOYMENT.md#вариант-3-nodejs-веб-сервер-serve)

4. **Автоматический деплой:**
   ```powershell
   .\deploy.ps1
   ```

Подробности, решение проблем и дополнительные настройки — в **[DEPLOYMENT.md](./DEPLOYMENT.md)**.
