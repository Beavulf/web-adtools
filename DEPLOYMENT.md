# 📦 Инструкция по деплою на локальный Windows сервер

Это руководство поможет вам развернуть WEB AD Tools на локальном Windows сервере.

## 📋 Подготовка

### 1. Сборка проекта для production

Перед деплоем необходимо собрать production-версию приложения:

```bash
# Установка зависимостей (если еще не установлены)
npm install

# Создание production-сборки
npm run build
```

После выполнения команды `npm run build` в папке `dist/` будут находиться оптимизированные статические файлы для размещения на веб-сервере.

### 2. Настройка переменных окружения для production

Перед сборкой убедитесь, что у вас настроены переменные окружения. Создайте файл `.env.production` в корне проекта:

```env
VITE_APP_URI_GRAPHQL=http://your-server-ip:4000/graphql
```

**Важно:** 
- Замените `your-server-ip` на IP-адрес или домен вашего сервера с бэкендом
- Убедитесь, что порт и путь к GraphQL endpoint указаны правильно
- Если бэкенд на том же сервере, можно использовать `http://localhost:4000/graphql`

### 3. Пересборка с production переменными

```bash
npm run build
```

## 🌐 Варианты веб-серверов

Выберите один из предложенных вариантов для размещения приложения:

---

## Вариант 1: IIS (Internet Information Services) - Рекомендуется для Windows

### Преимущества:
- Встроен в Windows Server
- Интеграция с Active Directory
- Удобное управление через GUI
- Поддержка SSL сертификатов

### Шаги установки:

#### 1. Установка IIS

1. Откройте **Панель управления** → **Программы** → **Включение или отключение компонентов Windows**
2. Найдите **Службы IIS** (Internet Information Services)
3. Установите следующие компоненты:
   - ✓ Веб-сервер (IIS)
   - ✓ Средства управления веб-сервером (IIS)
   - ✓ URL Rewrite Module (важно для SPA!)

#### 2. Установка URL Rewrite Module

URL Rewrite необходим для корректной работы React Router в SPA.

1. Скачайте **IIS URL Rewrite Module** с [официального сайта](https://www.iis.net/downloads/microsoft/url-rewrite)
2. Установите скачанный файл
3. Перезапустите IIS

#### 3. Размещение файлов

1. Скопируйте содержимое папки `dist/` в папку вашего сайта (например: `C:\inetpub\wwwroot\web-adtools\`)
2. Убедитесь, что папка доступна для чтения

#### 4. Создание сайта в IIS

1. Откройте **Диспетчер IIS** (IIS Manager)
2. Правой кнопкой на **Sites** → **Add Website**
3. Заполните:
   - **Site name**: `web-adtools`
   - **Physical path**: `C:\inetpub\wwwroot\web-adtools` (путь к вашим файлам)
   - **Binding**:
     - Type: `http` или `https`
     - IP address: `All Unassigned` или конкретный IP
     - Port: `80` (или другой порт, например `8080`)
     - Host name: оставьте пустым или укажите домен

#### 5. Настройка web.config для SPA

Создайте файл `web.config` в корне папки `dist/` (вместе с `index.html`):

```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <system.webServer>
    <rewrite>
      <rules>
        <!-- Редирект всех запросов на index.html для поддержки React Router -->
        <rule name="React Router" stopProcessing="true">
          <match url=".*" />
          <conditions logicalGrouping="MatchAll">
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
          </conditions>
          <action type="Rewrite" url="/" />
        </rule>
      </rules>
    </rewrite>
    
    <!-- Настройки безопасности -->
    <httpProtocol>
      <customHeaders>
        <remove name="X-Powered-By" />
      </customHeaders>
    </httpProtocol>
    
    <!-- Кэширование статических файлов -->
    <staticContent>
      <clientCache cacheControlMode="UseMaxAge" cacheControlMaxAge="365.00:00:00" />
    </staticContent>
  </system.webServer>
</configuration>
```

#### 6. Настройка прав доступа

1. В **Диспетчере IIS** выберите ваш сайт
2. Откройте **Authentication**
3. Убедитесь, что **Anonymous Authentication** включена
4. Проверьте, что пользователь `IIS_IUSRS` имеет права на чтение папки

#### 7. Проверка работы

Откройте браузер и перейдите по адресу: `http://your-server-ip:80` (или указанный вами порт)

---

## Вариант 2: Nginx for Windows

### Преимущества:
- Легковесный и быстрый
- Простая конфигурация
- Хорошая производительность

### Шаги установки:

#### 1. Установка Nginx

1. Скачайте Nginx для Windows с [официального сайта](http://nginx.org/en/download.html)
2. Распакуйте архив (например, в `C:\nginx\`)

#### 2. Размещение файлов

1. Скопируйте содержимое папки `dist/` в `C:\nginx\html\web-adtools\`

#### 3. Настройка конфигурации

Откройте файл `C:\nginx\conf\nginx.conf` и добавьте следующий блок:

```nginx
server {
    listen 80;
    server_name localhost;  # или IP вашего сервера
    
    root C:/nginx/html/web-adtools;
    index index.html;
    
    # Поддержка SPA - все запросы идут на index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Кэширование статических файлов
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # Отключение логирования для favicon
    location = /favicon.ico {
        log_not_found off;
        access_log off;
    }
    
    # Gzip сжатие
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;
}
```

#### 4. Запуск Nginx

Откройте командную строку от имени администратора:

```bash
cd C:\nginx
nginx.exe
```

#### 5. Проверка работы

Откройте браузер: `http://localhost:80`

**Управление Nginx:**
- Остановка: `nginx.exe -s stop`
- Перезагрузка: `nginx.exe -s reload`

---

## Вариант 3: Node.js веб-сервер (serve) - Для быстрого тестирования

### Преимущества:
- Простота настройки
- Не требует установки дополнительного ПО

### Установка и запуск:

#### 1. Установка пакета serve

```bash
npm install -g serve
```

#### 2. Запуск сервера

```bash
cd dist
serve -s . -l 80
```

Опции:
- `-s` - режим SPA (автоматические редиректы на index.html)
- `-l 80` - порт 80 (можно указать другой)

#### 3. Автозапуск через Windows Service

Для запуска как службы Windows можно использовать [node-windows](https://github.com/coreybutler/node-windows):

```bash
npm install -g node-windows
```

Затем создайте скрипт для установки службы.

---

## 🔧 Общие настройки после деплоя

### Настройка CORS на бэкенде

Убедитесь, что ваш NestJS бэкенд настроен для работы с фронтендом. В настройках CORS добавьте адрес вашего фронтенда:

```typescript
// Пример для NestJS
app.enableCors({
  origin: 'http://your-server-ip:80',
  credentials: true,
});
```

### Проверка переменных окружения

После деплоя проверьте, что приложение корректно обращается к бэкенду:
1. Откройте DevTools в браузере (F12)
2. Перейдите на вкладку **Network**
3. Попробуйте войти в систему
4. Убедитесь, что GraphQL запросы отправляются на правильный адрес

### Настройка файервола Windows

Если приложение недоступно извне, настройте файервол:

1. Откройте **Панель управления** → **Брандмауэр Защитника Windows**
2. Нажмите **Дополнительные параметры**
3. Создайте новое правило для входящих подключений:
   - Тип: Порт
   - Протокол: TCP
   - Порт: 80 (или ваш порт)
   - Действие: Разрешить подключение

---

## 🔒 Настройка HTTPS (SSL)

Для production рекомендуется использовать HTTPS. Для этого:

### Вариант A: Использование Let's Encrypt (через win-acme)

1. Скачайте [win-acme](https://www.win-acme.com/)
2. Следуйте инструкциям для получения SSL сертификата
3. Настройте IIS для использования SSL сертификата

### Вариант B: Самоподписанный сертификат (для тестирования)

1. В IIS Manager выберите ваш сайт
2. Откройте **Bindings**
3. Добавьте новую привязку:
   - Type: `https`
   - Port: `443`
   - SSL certificate: выберите или создайте самоподписанный сертификат

**Важно:** Самоподписанные сертификаты вызывают предупреждение в браузере.

---

## 🐛 Решение проблем

### Проблема: После перезагрузки страницы выдает 404

**Решение:** Это означает, что сервер не настроен для SPA. Убедитесь, что:
- Установлен и настроен URL Rewrite для IIS
- В nginx есть правило `try_files $uri $uri/ /index.html;`
- Для serve используется флаг `-s`

### Проблема: Не загружаются статические файлы (CSS, JS)

**Решение:**
- Проверьте пути к файлам в `index.html`
- Убедитесь, что в `vite.config.js` правильно настроен `base` (если приложение не в корне)
- Проверьте права доступа к файлам

### Проблема: CORS ошибки при запросах к GraphQL

**Решение:**
- Убедитесь, что в `.env.production` указан правильный URL бэкенда
- Проверьте настройки CORS на бэкенде
- Убедитесь, что бэкенд доступен по указанному адресу

### Проблема: Приложение не работает на другом порту

**Решение:**
- Измените порт в настройках сайта (IIS) или конфигурации (nginx)
- Обновите правила файервола для нового порта
- Перезапустите веб-сервер

---

## 📝 Чек-лист деплоя

- [ ] Проект собран через `npm run build`
- [ ] Создан файл `.env.production` с правильными настройками
- [ ] Файлы из `dist/` скопированы на сервер
- [ ] Веб-сервер установлен и настроен
- [ ] Настроены редиректы для SPA (web.config для IIS или try_files для nginx)
- [ ] Настроены права доступа к файлам
- [ ] Настроен CORS на бэкенде
- [ ] Открыты необходимые порты в файерволе
- [ ] Приложение доступно по указанному адресу
- [ ] Авторизация работает корректно
- [ ] GraphQL запросы выполняются успешно

---

## 🚀 Автоматизация деплоя (опционально)

Можно создать простой скрипт для автоматической сборки и копирования файлов:

### Скрипт PowerShell (deploy.ps1)

```powershell
# Сборка проекта
Write-Host "Building project..." -ForegroundColor Green
npm run build

# Копирование файлов на сервер
Write-Host "Copying files to server..." -ForegroundColor Green
$source = "dist\*"
$destination = "C:\inetpub\wwwroot\web-adtools\"

# Создание папки если не существует
if (!(Test-Path $destination)) {
    New-Item -ItemType Directory -Path $destination
}

Copy-Item -Path $source -Destination $destination -Recurse -Force

Write-Host "Deployment completed!" -ForegroundColor Green
```

Использование:
```powershell
.\deploy.ps1
```

---

## 📞 Дополнительная помощь

Если возникли проблемы:
1. Проверьте логи веб-сервера
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все зависимости установлены
4. Проверьте, что порты не заняты другими приложениями